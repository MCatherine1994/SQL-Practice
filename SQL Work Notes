1. Convert julian date to the calendar date: 
   TO_DATE(TO_NUMBER(1900000 + TABLE_COLOUMN, '9999999'), 'YYYYDDD'）  

2. Select distinct COLOUMN_NAME from TABLE_NAME  
  
3. Floor(n) returns largest integer equal to or less than n 
   Round(n, integer) round to the "integer" position, this places to the right of decimal point 
      
4. Table.Coloumn in (:listname) pops up a selection table (as a parameter) to let the user enter a value  
 
5. SYSDATE is the system date   

6. NVL(String1, replace_with) substitue a value when a null value is encountered

7. DECODE(TABLE.COLOUMN,
          'WHAT', 'REPRESENT WHAT',            //eg. '01', food.apple_price, '02', food.orange_price, '0'
          'WHAT', 'REPRESENT WHAT',
          'WHAT', 'REPRESENT WHAT',
          'WHAT') NAME     -------------Name wants to be called
          
8. Pop up a selectable window to let you enter the value
   TABLE.COLOUMN in (:ListName) 

9. (CASE TABLE.COLOUMN 
   WHEN ... THEN ...
   ELSE...
   END) Name

10. select * from TABLE limit 10

11. SELECT column1, column2, ...
FROM table_name
ORDER BY column1, column2, ... ASC|DESC;
